// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a minimal configuration.
  // For most App Router projects, the default behavior is sufficient.
  // Explicitly defining this file ensures Next.js build process is clearly configured.
};

module.exports = nextConfig;
